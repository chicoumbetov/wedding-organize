import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    ul: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
        margin: '2rem 23rem',
    },
}));
